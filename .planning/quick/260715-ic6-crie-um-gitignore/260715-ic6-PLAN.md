---
phase: quick
plan: ic6
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - QUICK-ic6
must_haves:
  truths:
    - .gitignore keeps existing useful entries (dist/, criminalsay)
    - Build artifacts, test/coverage outputs, IDE files, OS junk, and env secrets are ignored
    - Source files (main.go, internal/, data/, Makefile, go.mod) remain tracked/trackable
  artifacts:
    - .gitignore (expanded for Go CLI project)
  key_links:
    - Makefile BINARY=criminalsay and DIST=dist must remain ignored after make build / make all
    - Untracked .DS_Store files currently visible in git status must match ignore rules
---

<objective>
Expand the minimal `.gitignore` into a proper ignore file for this Go CLI so build outputs, coverage artifacts, IDE/OS junk, and secrets stay out of git — without dropping the useful existing entries.

Purpose: Stop noise (`.DS_Store`, local binaries, `dist/`) from polluting `git status` after normal `make build` / `make test` workflows.
Output: A single updated `.gitignore` at the repo root.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<!-- Current .gitignore (minimal):
     dist/
     criminalsay

     Repo: Go 1.22 CLI (github.com/allanmedeiros71/criminalsay).
     Build: `make build` → ./criminalsay; `make all` → dist/criminalsay-*.
     Untracked today: .DS_Store, internal/.DS_Store.
     Keep data/quotes.json, go.mod, go.sum, source, docs, and .planning/ trackable. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand .gitignore for Go CLI artifacts</name>
  <files>.gitignore</files>
  <action>
Rewrite/expand `.gitignore` at the repo root. Preserve the existing entries (`dist/` and `criminalsay`) and add clear sections covering this project's real tooling.

Required coverage (group with comments):

1. **Binaries / build** — keep `dist/` and `criminalsay`; also ignore common Go binary patterns if useful (`*.exe`, `*.exe~`, `*.dll`, `*.so`, `*.dylib`) so local and cross-built names do not leak outside `dist/`.

2. **Test / coverage** — `*.test`, `*.out`, `coverage.out`, `coverage.html`, `*.coverprofile`, and similar `go test -cover*` outputs.

3. **Go tooling / vendor (optional, keep light)** — ignore `vendor/` only if not used (this repo does not vendor); do **not** ignore `go.mod` / `go.sum`.

4. **IDE / editor** — `.idea/`, `*.swp`, `*.swo`, `*~`, `.vscode/` settings that are machine-local (prefer ignoring `.vscode/` unless the project relies on shared workspace files — this repo has none, so `.vscode/` is fine; also `.cursor/` only if it would ignore project-shared rules — do **not** blankedly ignore `.planning/` or `.claude/` project docs). Prefer: `.idea/`, `*.swp`, `*.swo`, `*~`.

5. **OS junk** — `.DS_Store`, `Thumbs.db`, `Desktop.ini`.

6. **Secrets / env** — `.env`, `.env.*`, `!.env.example` (if no example exists that is fine; still ignore `.env*`).

Do not ignore: `data/`, `internal/`, `main.go`, `Makefile`, `README*`, `LICENSE`, `go.mod`, `go.sum`, `.planning/`.

Keep the file concise and project-appropriate — not a bloated global template dump.
  </action>
  <verify>
    <automated>cd /Users/allan/git/criminalsay && test -f .gitignore && grep -q '^dist/' .gitignore && grep -q 'criminalsay' .gitignore && grep -q '\.DS_Store' .gitignore && grep -Eq 'coverage|\.out|\.test' .gitignore && grep -Eq '\.env|\.idea|Thumbs\.db' .gitignore && git check-ignore -q .DS_Store && git check-ignore -q dist/foo && git check-ignore -q criminalsay && ! git check-ignore -q main.go && ! git check-ignore -q data/quotes.json && ! git check-ignore -q go.mod</automated>
  </verify>
  <done>.gitignore still ignores dist/ and criminalsay; also covers .DS_Store, coverage/test outputs, IDE/OS junk, and .env files; tracked/source paths remain not ignored.</done>
</task>

</tasks>

<verification>
After execution:
1. `git check-ignore -v .DS_Store dist/ criminalsay` shows matches from `.gitignore`
2. `git status` no longer lists `.DS_Store` / `internal/.DS_Store` as untracked (unless already force-added)
3. `go test ./...` still passes (unchanged by this task; sanity only if desired)
</verification>

<success_criteria>
- One coherent `.gitignore` for this Go CLI
- Existing `dist/` + `criminalsay` entries retained
- Typical local clutter ignored; source and data remain trackable
</success_criteria>

<output>
After completion, create `.planning/quick/260715-ic6-crie-um-gitignore/260715-ic6-SUMMARY.md`
</output>
