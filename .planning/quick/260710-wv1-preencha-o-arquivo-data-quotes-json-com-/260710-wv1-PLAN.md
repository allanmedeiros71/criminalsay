---
phase: quick-260710-wv1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/quotes.json
autonomous: true
requirements:
  - CONTENT-01
must_haves:
  truths:
    - data/quotes.json contains quotes from all 15 seasons of Criminal Minds (seasons 1–15 represented)
    - every quote entry has all 6 required fields: quote, author, character, season, episode, episodeTitle
    - quote text is accurate — matches actual dialogue or opening/closing narration from the show
    - author attribution is correct — the real-world person (philosopher, author, etc.) who originated the quote
    - character field identifies the BAU agent who delivered the quote in the episode
    - the JSON file is valid and parses without error
  artifacts:
    - data/quotes.json (expanded, all seasons covered, valid JSON)
  key_links:
    - go:embed in the quotes package reads data/quotes.json at compile time — malformed JSON will cause build failure
    - the existing 20 entries must be preserved; new entries are appended
---

<objective>
Expand data/quotes.json from 20 entries to a rich dataset covering all 15 seasons of Criminal Minds (seasons 1–15). Each entry must follow the established schema exactly. The file is embedded at compile time via go:embed, so it must remain valid JSON throughout.

Purpose: CriminalSay's value scales with the breadth and quality of its quote library. A single-season or thin dataset undercuts the "random memorable quote" experience. Covering all 15 seasons ensures every session opening feels fresh.

Output: data/quotes.json with 120+ entries (roughly 8–10 quotes per season), all fields populated, valid JSON, preserving all 20 existing entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@data/quotes.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research and collect verified Criminal Minds quotes for all 15 seasons</name>
  <files>data/quotes.json</files>
  <action>
Use web search to collect verified quotes from Criminal Minds seasons 1 through 15. For each season, gather at least 6–8 opening/closing narration quotes that were actually spoken by BAU agents in the show.

Search strategy — use multiple targeted queries:
- "Criminal Minds season [N] opening quotes site:imdb.com OR site:quotes.net OR site:wikiquote.org"
- "Criminal Minds season [N] quotes Spencer Reid Hotchner Morgan Rossi Prentiss JJ Garcia"
- Cross-reference found quotes against episode lists to confirm season/episode numbers and titles

Schema for each entry (must match exactly — no extra fields, no missing fields):
- "quote": the exact text of the literary/philosophical quote as read in the episode
- "author": the real-world person who wrote or said the quote (e.g., "Marcus Aurelius", "Friedrich Nietzsche"); if the quote is original dialogue by a character, set author to the character's full name
- "character": the BAU agent who delivered the quote in the episode (e.g., "Spencer Reid", "Aaron Hotchner", "David Rossi", "Derek Morgan", "Emily Prentiss", "Jennifer Jareau", "Penelope Garcia", "Jason Gideon", "Alex Blake", "Kate Callahan", "Luke Alvez", "Tara Lewis", "Matt Simmons")
- "season": integer (1–15)
- "episode": integer (episode number within the season)
- "episodeTitle": string (exact episode title)

Seasons 1–12 are Criminal Minds (original series). Seasons 13–15 continue the original. The existing 20 entries cover seasons 1–12 sparsely; focus new research on seasons that have fewer than 3 existing entries and fill all gaps including seasons 13, 14, and 15.

Existing entries to preserve (do NOT duplicate these season/episode combinations):
- S1E1 (two entries), S1E22, S2E1, S2E8, S3E1, S3E9, S3E11, S4E1, S4E13, S5E6, S5E9, S6E1, S6E18, S7E1, S8E1, S9E1, S9E13, S11E22, S12E22

After collecting all new quotes, write the complete updated data/quotes.json. The file must:
1. Start with `[` and end with `]` — it is a JSON array
2. Preserve all 20 existing entries exactly as they are
3. Append new entries after the existing ones
4. Have no trailing comma after the last entry
5. Use 2-space indentation (match existing style)
6. Contain at least 100 total entries when done
  </action>
  <verify>
    <automated>cd /Users/allan/git/criminalsay && python3 -c "import json; data=json.load(open('data/quotes.json')); seasons=set(q['season'] for q in data); missing=[s for s in range(1,16) if s not in seasons]; assert not missing, f'Missing seasons: {missing}'; assert len(data) >= 100, f'Only {len(data)} entries'; required=['quote','author','character','season','episode','episodeTitle']; bad=[i for i,q in enumerate(data) if any(k not in q for k in required)]; assert not bad, f'Missing fields at indices: {bad}'; print(f'OK: {len(data)} quotes, seasons {sorted(seasons)}')"</automated>
  </verify>
  <done>data/quotes.json contains 100+ entries with all 15 seasons represented, every entry has all 6 required fields, and the file parses as valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the expanded JSON compiles into the Go binary</name>
  <files>data/quotes.json</files>
  <action>
Run the Go build to confirm the expanded data/quotes.json is accepted by go:embed and the binary compiles without errors. This catches any JSON syntax errors that the Python parse check might miss due to encoding edge cases, and confirms the embed path is still correct.

Run: `go build ./...` from the project root. If the build fails with a JSON decode error at runtime (not compile time), also run `go test ./...` to surface the decode failure.

If the build succeeds, optionally run the binary once to confirm a quote is displayed: `./criminalsay` (or `go run main.go`).
  </action>
  <verify>
    <automated>cd /Users/allan/git/criminalsay && go build ./... && echo "BUILD OK"</automated>
  </verify>
  <done>`go build ./...` exits 0. The binary compiles cleanly with the expanded quotes dataset embedded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSearch → data/quotes.json | Quote text sourced from external sites; incorrect attribution or fabricated quotes could enter the dataset |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-wv1-01 | Tampering | quote attribution (author field) | medium | mitigate | Cross-reference each author attribution against at least two independent sources before writing |
| T-wv1-02 | Information Disclosure | quote accuracy | low | accept | Misquotes are a cosmetic defect, not a security issue; accuracy is a quality concern handled by verification |
</threat_model>

<verification>
1. `python3 -c "import json; ..."` — validates JSON syntax, all-seasons coverage, field completeness, minimum count (100 entries)
2. `go build ./...` — confirms go:embed accepts the file and the binary links
3. Manual spot-check: run `./criminalsay` several times to confirm quotes from different seasons appear
</verification>

<success_criteria>
- data/quotes.json is valid JSON (no parse errors)
- All 15 seasons (1–15) have at least one entry
- Total entry count >= 100
- Every entry contains exactly the 6 required fields with correct types (season/episode as integers, others as strings)
- `go build ./...` exits 0
</success_criteria>

<output>
Create `.planning/quick/260710-wv1-preencha-o-arquivo-data-quotes-json-com-/260710-wv1-SUMMARY.md` when done
</output>
